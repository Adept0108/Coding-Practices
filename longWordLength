 int i,count=0,maxcount=0;
 
 for ( i = 0 ; *(s+i)!='\0' ; i ++ ) {
     if (isalpha(*(s+i))!=0) count++;
     
     else{
         if (count>maxcount) 
             maxcount=count;
         count =0 ; 
         
     }
 }
 return maxcount;
